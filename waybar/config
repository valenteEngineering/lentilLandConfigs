{
    //- The bar's position and other settings
    "layer": "top",
    "position": "bottom",
    //"height": 1,
    "spacing": 40,

    //- The modules that will be displayed
    "modules-left": [
        "hyprland/workspaces",
        "hyprland/mode" // This will be used for your resize mode indicator!
    ],
    "modules-center": ["hyprland/window"],
    "modules-right": [
        "pulseaudio",
        "custom/network_status",
        "cpu",
        "memory",
        "clock"
    ],

    //- Configuration for each module
    "hyprland/workspaces": {
        "format": "{name}",
        "on-click": "activate"
    },
    "hyprland/window": {
        "format": "{}",
        "max-length": 50
    },
    "pulseaudio": {
        "format": "{volume}% ", // You'll need a Nerd Font for the icon
        "format-muted": "Muted ",
        "on-click": "pavucontrol"
    },

    "custom/network_status": {
        "exec": "cat /tmp/waybar_net_data.json",
        "interval": 1,
    },


//    "network": {
//        "format-wifi": "{essid} ({signalStrength}%) ",
//        "format-ethernet": "{ifname}: {ipaddr}/{cidr} ",
//        "format-disconnected": "Disconnected "
//    },

    "cpu": {
        "format": "{usage}% ",
        "tooltip": true
    },
    "memory": {
        "format": "{}% "
    },
    "clock": {
        "format": "{:%I:%M %p}", // 12-hour format
        "tooltip-format": "<big>{:%Y-%m-%d}</big>\n<tt><small>{calendar}</small></tt>"
    }
}
